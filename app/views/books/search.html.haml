- provide(:title, 'Book')
%h1 First, let's add a title

.row
  .col-md-6.col-md-offset-3
    = form_with(url: new_book_path, scope: :query, local: true) do |f|
      = f.label :title
      = f.text_field :title, class: 'form-control'

      = f.submit 'Next', class: 'btn btn-primary'
    %p
      = link_to 'Back', home_path
